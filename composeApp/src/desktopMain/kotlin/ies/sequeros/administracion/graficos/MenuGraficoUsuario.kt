package ies.sequeros.administracion.graficos

import androidx.compose.runtime.Composable

@Composable
fun MenuGraficoUsuario() {
    /*val navController = rememberNavController()
    NavHost(
        navController = navController,
        startDestination = "usuarioprincipal" // Pantalla inicial
    ) {
        composable("principal") {
            Principal(navController)
        }
        composable("tienda") {
            Tienda()//navController)
        }
        composable("administracion") {
            Administracion() //(navController)
        }
    }*/
   // EjemploGrafico(usuarioViewModel)
}
